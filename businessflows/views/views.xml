<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="businessflows.list">
      <field name="name">businessflows list</field>
      <field name="model">businessflows.businessflows</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="businessflows.action_window">
      <field name="name">businessflows window</field>
      <field name="res_model">businessflows.businessflows</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="businessflows.action_server">
      <field name="name">businessflows server</field>
      <field name="model_id" ref="model_businessflows_businessflows"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="businessflows" id="businessflows.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="businessflows.menu_1" parent="businessflows.menu_root"/>
    <menuitem name="Menu 2" id="businessflows.menu_2" parent="businessflows.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="businessflows.menu_1_list" parent="businessflows.menu_1"
              action="businessflows.action_window"/>
    <menuitem name="Server to list" id="businessflows" parent="businessflows.menu_2"
              action="businessflows.action_server"/>
    -->
  </data>
</odoo>